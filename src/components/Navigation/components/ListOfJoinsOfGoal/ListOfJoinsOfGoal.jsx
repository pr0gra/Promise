import { StyleSheet, Text, View } from "react-native";
import React from "react";

export const ListOfJoinsOfGoal = ({ isVisible, setIsVisible, navigation }) => {
  return (
    <View>
      <Text>ListOfJoinsOfGoal</Text>
    </View>
  );
};

const styles = StyleSheet.create({});
